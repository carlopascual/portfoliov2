import Item, { BackItem } from "../components/item";
import Header from "../components/header";

const ExperienceUnity = ({ nextScreen }) => {
  return (
    <>
      <Header style={{ fontSize: "9vw", marginTop: "4vw" }}>
        Unity Technologies
      </Header>
    </>
  );
};

export default ExperienceUnity;
